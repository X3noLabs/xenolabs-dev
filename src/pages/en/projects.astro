---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProjectMockup from '../../components/ProjectMockup.astro';
import { projects } from '../../data/projects';

const lang = 'en';
---

<BaseLayout
  lang={lang}
  title="Projects"
  description="A look at what I've built — from an AI voice assistant for the car to internal tools and client websites."
>
  <section class="container-page py-20 sm:py-28">
    <div class="max-w-2xl">
      <p class="text-sm font-medium text-accent-light">Projects</p>
      <h1 class="mt-4 font-display text-4xl font-medium tracking-tight text-fg sm:text-5xl">A few things I've built.</h1>
      <p class="mt-5 text-fg/60">
        A mix of personal projects and client work — real screenshots where I
        have them, a placeholder view of the interface where I don't yet.
      </p>
    </div>

    <div class="mt-14 space-y-10">
      {projects.map((project) => (
        <article id={project.slug} class="scroll-mt-24 grid gap-6 rounded-2xl border border-fg/10 bg-surface p-6 sm:grid-cols-2 sm:p-8">
          <div class="aspect-[8/5] overflow-hidden rounded-xl">
            {project.screenshot ? (
              <img src={project.screenshot} alt={`${project.name[lang]} screenshot`} class="h-full w-full object-cover object-top" loading="lazy" />
            ) : (
              <ProjectMockup name={project.name[lang]} accent={project.accent} />
            )}
          </div>
          <div class="flex flex-col justify-center">
            <div class="flex flex-wrap gap-2">
              {project.stack.map((tech) => (
                <span class="rounded-full bg-fg/5 px-2.5 py-1 text-xs font-medium text-fg/60">{tech}</span>
              ))}
            </div>
            <h2 class="mt-3 font-display text-xl font-medium text-fg">{project.name[lang]}</h2>
            <p class="mt-1 text-sm font-medium text-accent-light">{project.tagline[lang]}</p>
            <p class="mt-3 text-fg/60">{project.description[lang]}</p>
          </div>
        </article>
      ))}
    </div>
  </section>
</BaseLayout>
